def calculator(first,second,**options):
	if options.get("name"):
		print "user name is %s"%options.get("name")
	if options.get("action")=="sum":
		return first+second
	if options.get("action")=="minus":
		return first-second
	if options.get("action")=="multiply":
		return first*second

print calculator(4,5,action="multiply",name="pankaj sharma")
